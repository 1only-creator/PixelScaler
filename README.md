SOON

Usage

    Run chmod +x install.sh && ./install.sh.

    Activate venv: source venv/bin/activate.

    Compile the C++ core: g++ src/engine.cpp -o engine pkg-config --cflags --libs opencv4``.

    Run the controller: python main.py.
